*** fetching data from nenoe PostrresSQL ****

1- on the home page get the user session: 
const session = await auth()

2- fetch from db   
  const latestBooks = (await 
    db.select()
    .from(books)
    .limit(10)
    .orderBy(desc(books.createdAt))
  ) as Book[];

